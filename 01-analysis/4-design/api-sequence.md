# Sequence-диаграмма API-взаимодействия

> Этап 3. Проектирование. Как клиент и сервер обмениваются вызовами в ключевом сценарии
> (напр. бронирование слота). Контракты API — в [api/openapi.yaml](../../api/openapi.yaml).

## Сценарий: _Название_

```mermaid
sequenceDiagram
    %% TODO: опишите обмен сообщениями
    %% actor User
    %% participant App
    %% participant API
    %% User->>App: ...
    %% App->>API: ...
    %% API-->>App: ...
```
